# Project Specification: Equity Analysis Dashboard
**Course:** CSCI-2025: Data Manipulation and Visualization (Winter Term)  
**Client:** Mark Heidrich, Associate Vice President for Institutional Effectiveness
and Registrar

## 1. Project Overview
The class will function as a single data science consultancy divided into four "Feature Pods." The goal is to build **one unified Shiny Dashboard** that allows academic departments to analyze equity gaps in their programs.

Instead of building four separate apps, we are building four **Modules** that integrate into a single "Golden Github Repository."

## 2. Team Structure

### The Four Teams
The class is divided into four teams. Each team owns one "tab" of the final application.

1.  **Pod 1:** Enrollment
2.  **Pod 2:** Performance
3.  **Pod 3:** Retention
4.  **Pod 4:** Outcomes

### Individual Roles
Every student has a specific title and responsibility within their team.

| Role | Responsibility | Key Deliverable |
| :--- | :--- | :--- |
| **GitHub Sentinel/Team Leader** (Teach Lead)| Manages the git branch, resolves merge conflicts, and manages `renv.lock`. | The clean Merge; Repo health. |
| **Data Steward** (Data Engineer) | ETL processes and **Privacy Protection**. Masks data if N < 5. | `clean_data.R`; Anonymized datasets. |
| **Chart Artist** (Viz Specialist) | Creates high-quality static visualizations. | Plot functions (e.g., `plot_gpa()`). |
| **Interface Architect** (Shiny Dev) | Wires the UI/Server logic. Connects inputs to the plots. | `ui` and `server` module code. |
| **Narrative Lead** (Context/QA) | Writes dynamic "Mad Libs" text to interpret charts automatically. | `renderText()` logic; Help tooltips. |

## 3. Collaboration: "The Guilds"
To ensure the dashboard looks cohesive, students across teams will meet in "Guilds".

* **The Code Guild (Sentinels):** Agrees on the Git branching strategy and dependency management.
* **The Data Guild (Stewards):** Agrees on variable naming conventions and the "Small N" suppression threshold.
* **The UI Guild (Chart Artists):** Decides aesthetic conventions for project (e.g. colors, fonts, themes, etc.).
* **The Shiny Guild (Interface Architects):** Standardizes UI layout and input types across modules.
* **The Narrative Guild (Narrative Leads):** Standardizes the "Mad Libs" text structure for consistency.

## 4. Technical Architecture

### The "Golden Repo"
Students will clone a repository pre-scaffolded by the instructor.

* **`global.R`**: Shared libraries and data loading (Read-Only for students).
* **`app.R`**: The main shell application (Read-Only).
* **`modules/`**:
    * `mod_access.R` (Team 1 Workspace)
    * `mod_gatekeeper.R` (Team 2 Workspace)
    * `mod_retention.R` (Team 3 Workspace)
    * `mod_outcome.R` (Team 4 Workspace)
* **`data/`**: Stores the clean data files.
* **`renv/`**: Manages package dependencies.

<!-- ## 5. The 7-Session "Sprint" Timeline

| Session | Phase | Key Activity |
| :--- | :--- | :--- |
| **Day 1** | **Foundation** | **The Data Contract.** Teams agree on variable names. Clone the repo. |
| **Day 2** | **Static Viz** | **Guild Meetings.** Work strictly in RMarkdown/Quarto to build static plots. |
| **Day 3** | **Construction** | **Shiny-fication.** Migrate static plots into Module UI. Focus on layout. |
| **Day 4** | **Reactivity** | **Wiring.** Implement `reactive()` logic (sliders, dropdowns) and "Mad Libs" text. |
| **Day 5** | **Integration** | **The Merge Party.** Teams PR their modules into `main`. Resolve conflicts. |
| **Day 6** | **Polish** | **Bug Smashing.** Fix UI inconsistencies, check tab order, refine CSS. |
| **Day 7** | **Delivery** | **Client Demo.** Final deployment and presentation to the client. | -->

## 6. Development Workflow Rules

1.  **Branching:** Use Trunk-Based Development. Branch from `main` -> `feature/team-name` -> PR back to `main`.
2.  **No Direct Pushes:** All code must enter `main` via a Pull Request (PR).
3.  **The "Sentinel" Rule:** Only the Team GitHub Sentinel is authorized to open a PR.
4.  **Dependency Lockdown:** Do not install new library packages without instructor approval. If approved, `renv::snapshot()` must be run immediately.

<!-- ## 7. Instructor Prep (Pre-Game)

Before Day 1, the Instructor must:
1.  **Create the Repository:** Initialize the RProject with `renv`.
2.  **Scaffold the Modules:** Create the empty `mod_teamX.R` files so students have a place to start.
3.  **Clean the Data (Partially):** Ensure the dataset is joinable, even if it requires further cleaning by the Data Stewards. -->