---
title: "CSCI-2025: Data Manipulation and Visualization"
execute: 
  freeze: false
---

This page contains an outline of the topics, content, and assignments for the semester. Note that this schedule will be updated as the semester progresses and the timeline of topics and assignments might be updated throughout the semester.

```{r}
#| echo: false
#| message: false
#| warning: false
library(googlesheets4)
library(gt)
library(tidyverse)
gs4_deauth()
d <- read_sheet("https://docs.google.com/spreadsheets/d/1MCjj00m98ARPOm8sfdKC3HIcvdai6NWGE8fjXgOJgt8/edit?usp=sharing")
```

```{r}
#| echo: false

d |>
  filter(Publish == "x") |>
  select(-dow, -Publish) |>
  mutate(due = as.Date(due)) |>
  gt() |>
  fmt_date(due, date_style = 31) |>
  sub_missing(columns = c(week, topic, reading, slides), missing_text = "") |>
  cols_align(
    align = "center", 
    columns = c(week)
  ) |>
  cols_align(
    align = "center", 
    columns = c(due)
  ) |>
  cols_align(
    align = "left", 
    columns = c(topic, reading, slides)
  ) |>
  tab_style(
    style = cell_borders(
      sides = "right",
      color = "#D3D3D3",
      style = "solid"
    ),
    locations = cells_body(
      columns = c(due, type, topic, reading, slides)
    )
  ) |>
  fmt_markdown(
    columns = c(type, topic, reading, slides)
  ) |>
  cols_width(
    week ~ px(45),
    due ~ px(300), 
    reading ~ px(150),
    topic ~ px(300),
    slides ~ px(300)
  ) |>
  cols_label_with(fn = function(x) {
      janitor::make_clean_names(x, case = "title") |>
        toupper() |>
        stringr::str_replace_all("^|$", "**") |>
        md()
    }) |>
  tab_options(table.font.size = 13) |>
  opt_row_striping()
```
